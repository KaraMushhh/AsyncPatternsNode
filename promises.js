/*function doAsyncWork(resolve, reject) {
    //permorm async calls
    if(success) {
        resolve(data);
    }
    else {
        reject(reason)
    }
}

let myPromise = new Promise((resolve, reject) =>{
    //permorm async calls
    if(success) {
        resolve(data);
    }
    else {
        reject(reason)
    }
}
);

methodThatReturnPromise()
 .then(data => console.log(data))
 catch(error => console.log(error))
 .finally(() => console.log('alld done')); */
